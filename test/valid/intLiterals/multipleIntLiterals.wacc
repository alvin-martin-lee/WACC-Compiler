# simple test with all types of int literals

# Output:
# #empty#

# Exit:
# 15

# Program:

begin
    int x = -0b101 ;     # x = -5
    x = x + 010 ;        # x += 8      = 3
    x = x + -19 ;        # x -= 19     = -16
    x = x + 0x1F ;       # x += 31     = 15
    exit x
end
